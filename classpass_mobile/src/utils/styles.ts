import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  signInButton: {
    marginBottom: 10,
  },
  input: {
    height: 40,
    minWidth: 200,
    margin: 12,
    borderWidth: 1,
    padding: 10,
  },
});

export default styles;
